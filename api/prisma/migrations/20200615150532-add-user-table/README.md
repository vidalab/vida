# Migration `20200615150532-add-user-table`

This migration has been generated by Phuoc Do at 6/15/2020, 3:05:32 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."User" (
"auth0Id" TEXT NOT NULL  ,"email" TEXT NOT NULL  ,"id" INTEGER NOT NULL  PRIMARY KEY AUTOINCREMENT)

CREATE TABLE "quaint"."new_Dashboard" (
"createdAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,"id" TEXT NOT NULL  ,"json" TEXT NOT NULL  ,"name" TEXT NOT NULL  ,"ownerEmail" TEXT NOT NULL DEFAULT '' ,"updatedAt" DATE NOT NULL DEFAULT CURRENT_TIMESTAMP ,"userId" INTEGER NOT NULL DEFAULT 0 ,
    PRIMARY KEY ("id"),FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE)

INSERT INTO "quaint"."new_Dashboard" ("createdAt", "id", "json", "name", "ownerEmail", "updatedAt") SELECT "createdAt", "id", "json", "name", "ownerEmail", "updatedAt" FROM "quaint"."Dashboard"

PRAGMA foreign_keys=off;
DROP TABLE "quaint"."Dashboard";;
PRAGMA foreign_keys=on

ALTER TABLE "quaint"."new_Dashboard" RENAME TO "Dashboard";

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "quaint"."User.auth0Id" ON "User"("auth0Id")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200612154843-add-owner-fields..20200615150532-add-user-table
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource DS {
   provider = "sqlite"
-  url = "***"
+  url      = env("DATABASE_URL")
 }
 generator client {
   provider      = "prisma-client-js"
@@ -11,9 +11,17 @@
 model Dashboard {
   id          String  @id @default(cuid())
   name        String
   json        String
-  createdAt   DateTime @default(now())
-  updatedAt   DateTime @default(now())
-  ownerId     String  @default("")
-  ownerEmail  String  @default("")
+  createdAt   DateTime  @default(now())
+  updatedAt   DateTime  @default(now())
+  ownerEmail  String    @default("")
+  userId      Int     @default(0)
+  user        User    @relation(fields: [userId], references: [id])
+}
+
+model User {
+  id          Int       @default(autoincrement()) @id
+  email       String    @unique
+  auth0Id     String    @unique
+  dashboards  Dashboard[]
 }
```


